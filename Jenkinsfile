pipeline {
    agent any

    stages {
        stage('Sum of Two Numbers') {
            steps {
                script {
                    int a = 10
                    int b = 20
                    int sum = a + b
                    echo "Sum of ${a} and ${b} is: ${sum}"
                }
            }
        }
    }
}
